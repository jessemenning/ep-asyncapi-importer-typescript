.. _install-content-install:

Prerequisites
=============

The importer requires `node 16.x`.

Install Globally
================

.. code-block:: bash

  npm install @solace-iot-team/sep-async-api-importer -g
